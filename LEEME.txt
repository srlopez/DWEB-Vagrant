GUION DE TRABAJO
1.
Inicializamos con 

vagrant init ubuntu/focal64
vagrant box update
vagrant box list

Vemos lo que contiene Vagrantfile, luego editamos el archivo y 

vagrant up
vagrant ssh

vemos que compartimos el directorio..., la ip, el tipo de red que nos asigna, etc...

vagrant halt
vagrant destroy
vagrant suspend
vagrant resume

=====
share, reload, provision
snapshot push, snapshot restore

https://gist.github.com/wpscholar/a49594e2e2b918f4d0c4

2.
Preparamos el script para
- nombrar la máquina virtual y asignarla a un grupo
- establecer la mem,cpu
- establecer la ip, y el hostname
- instalar apache ¿?

y probamos que funciona

3.
Configuramos para cliente, un dns, dos apaches, y un nginx
Trabajo de alumno